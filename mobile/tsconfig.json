{
	"//": "Solution file for `tsc -b`. The scaffold shipped `typecheck` without one, so the script failed on TS5083 before it type-checked anything. Apps are excluded: they are noEmit leaf configs, not composite projects, and are checked by their own `yarn workspace <app> typecheck`.",
	"files": [],
	"references": [
		{ "path": "./packages/domain-types" },
		{ "path": "./packages/api-client" },
		{ "path": "./packages/outbox" },
		{ "path": "./packages/tokens" },
		{ "path": "./packages/ui" }
	]
}
