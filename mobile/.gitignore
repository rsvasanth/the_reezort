node_modules/
.expo/
dist/
*.tsbuildinfo
.env.local

# Generated by `expo prebuild`. Regenerated on every build, so tracking it means
# merge conflicts in code nobody edits — and the config plugins in
# apps/*/plugins are the actual source of truth for what lands in it.
android/
ios/

# Build output
*.apk
*.aab
