.DS_Store
*.pyc
*.egg-info
*.swp
tags
node_modules
__pycache__

# JS/TS build artifacts
*.tsbuildinfo
packages/*/dist
apps/*/dist

# Expo / React Native
.expo/
apps/*/android/build/
apps/*/android/app/build/
apps/*/android/.gradle/
apps/*/ios/build/

# Expo prebuild output — generated from app.json + config plugins, never edited by hand.
# apps/guest already omits it; apps/ops was committed at RN 0.81 and silently rotted
# against the RN 0.85 upgrade (hermesc moved), which broke the build.
apps/*/android/
apps/*/ios/
